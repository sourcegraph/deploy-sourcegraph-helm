apiVersion: v1
data:
  grafana-datasources-pgsql.yml: |
    apiVersion: 1

    datasources:
      - name: pgsql
        type: postgres
        url: $PGHOST:$PGPORT
        user: $PGUSER
        database: $PGDATABASE
        secureJsonData:
          password: $PGPASSWORD
        jsonData:
          sslmode: $PGSSLMODE

kind: ConfigMap
metadata:
  labels:
    deploy: sourcegraph
    app.kubernetes.io/component: grafana
  name: grafana-datasources-pgsql
