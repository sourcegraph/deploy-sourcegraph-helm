# Demonstrate using external redis instance(s)
# Disables deployment of the internal `redis-cache` and `redis-store` deployment

redisCache:
  enabled: false
  connection:
    endpoint: redis://redis-cache.example.com:6379/2

redisStore:
  enabled: false
  connection:
    endpoint: redis://redis-store.example.com:6379
