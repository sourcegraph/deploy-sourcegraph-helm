apiVersion: v1
data:
  daemon.json: |
    { "insecure-registries":["private-docker-registry:5000"] }

kind: ConfigMap
metadata:
  labels:
    deploy: sourcegraph
    app.kubernetes.io/component: executor
  name: docker-config
