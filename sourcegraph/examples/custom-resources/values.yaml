customValue: 3

sg: # Matches dependency name in Chart.yaml
  sourcegraph: # Shown here to emphasize indentation
    podLabels:
      new-label: "custom" # Added to all pods

  pgsql:
    existingConfig: "pgsql-conf" # Provide a custom config file

